# RevPilot AI — API dependencies
# Every one of these is free and open source. No paid service, no broker,
# no Docker, no Redis, no Postgres (workflow.md ADL-003, ADL-009).

# --- web ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.3            # SSE live pipeline (§18.2)

# --- validation & config ---
pydantic>=2.9.0
pydantic-settings>=2.5.0

# --- persistence ---
sqlalchemy>=2.0.35
aiosqlite>=0.20.0
alembic>=1.13.0

# --- scheduling (in-process; no Celery, no broker) ---
apscheduler>=3.10.4

# --- integrations ---
httpx>=0.27.0
razorpay>=1.4.2
google-genai>=0.3.0             # Gemini free tier (§4.4)

# --- observability ---
structlog>=24.4.0

# --- test & quality ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
hypothesis>=6.112.0             # property-based policy-firewall proof (§15.3)
respx>=0.21.1                   # HTTP mocked at the transport layer
coverage>=7.6.0
ruff>=0.7.0
mypy>=1.11.0
